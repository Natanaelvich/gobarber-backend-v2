describe('CreateApppointment', () => {
  it('should be able to create a new appoitment', () => {
    expect(1 + 2).toBe(3);
  });

  it('should not be able to create a new appoitment on the same time', () => {
    expect(1 + 2).toBe(3);
  });
});
